TO DO:
    - Segmentation fault when sending -create with no input block size
    - Shared n does not attach memory blocks to process
    - Change malloc logic
    - Fix --help commands
    - Fix segmentation fault in cmd_free when given an invalid address
    - Pass atoi to strtol
    - Add a header when listing memoery blocks when doing malloc, mmap and shared
